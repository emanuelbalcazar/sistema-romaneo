(function () {
  'use strict';
    angular.module('controllerModule')
    .controller('messageCtrl', ['$scope', '$rootScope', '$location', '$interval', 'loggerSrv', messageCtrl]);

    // Controlador de la pantalla de mensajes.
    function messageCtrl($scope, $rootScope, $location, $interval, loggerSrv) {
        


    } // end messageCtrl
})();
